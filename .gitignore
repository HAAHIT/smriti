node_modules/
dist/
.output/
.wxt/
*.tsbuildinfo
*.log
.env
.env.local

# Claude Code local-only settings (machine-specific permissions)
.claude/settings.local.json

# Helper runtime artifacts
packages/helper/data/
packages/helper/*.db
packages/helper/*.db-journal
packages/helper/*.db-wal
packages/helper/*.db-shm

# Vendored embedding model + ONNX runtime (fetched via `npm run fetch:model`)
packages/extension/public/models/
packages/extension/public/ort/

# Rollup bundle-analysis output, regenerated by every build
packages/extension/stats.html

# Cloudflare Worker (sync-relay) local state
.wrangler/
.dev.vars
