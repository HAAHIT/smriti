{
  "name": "@smriti/sync-relay",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Zero-knowledge encrypted-blob relay for Smriti memory sync (Cloudflare Worker + KV).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "wrangler": "^3.78.0"
  }
}
